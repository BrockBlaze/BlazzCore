# BlazzCore — zram compressed swap
# Creates a compressed RAM-backed swap device (up to 4 GB or half of RAM).
# This significantly reduces memory pressure on live systems.

[zram0]
zram-size = min(ram, 4096)
compression-algorithm = zstd
